var ruta = navigator;
// var ruta = console;
// var ruta = window; 
// se pueden ver detalles de los objetos (conjuntos de funciones) y sus atributos(variables), con console.log
console.log(ruta);
document.write("Estas en: " + ruta);